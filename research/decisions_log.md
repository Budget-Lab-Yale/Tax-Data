---
title: "State-tax research decisions log"
role: status
workstream: cross-cutting
status: current
updated: 2026-09-14
sot: self
supersedes: []
superseded_by: null
---

# Decisions log — state-tax research

Records the operative decisions for the state-tax workstreams and their rationale, so a
question settled once is not re-argued from a stale document. Add a dated row when a
decision is made, changed, or revisited. Same format as the sibling Affordability-Index
repo's `docs/decisions_log.md`, so the two read alike.

Legend — **Status:** `set` (decided, reflected in the tree) · `provisional` (decided for
now, revisit) · `open` (not yet resolved).

The **S-series** covers both workstreams. Where a row settles *which document owns a
claim*, the substantive question is unchanged: it moves to one home, it is not re-decided.

---

## Contradictions closed 2026-08-19

These five were live disagreements between documents, found by an inventory of the corpus
before the move to `research/`. Each is now stated once, in one place.

| ID | Decision | Status | Rationale / notes |
|----|----------|--------|-------------------|
| S1 | The **−7% filer-bias figure has two distinct roles** and both are live: it is *retired as the reconciliation basis*, and it *stands as an acceptance metric* for the non-filer rework. The acceptance definition lives in `research/state_weights/plan.md` §C8; every other site cites it rather than restating it. | set | `state_weights_fit_issues.md` recorded "**Superseded as the reconciliation basis (2026-07-13)**: per JI, reconciliation now runs at the individual level (`compare_individuals_acs_irs()`), which is model-free" — while the design memo §3.2.6, plan §C8 and `STATUS.md`'s headline all used the same number as the bar the rework must close. Both readings are correct about different things: reconciliation is how we *compare* ACS to IRS, acceptance is what the filing model must *achieve*. Nothing substantive changes; the definition stops being maintained in four places. |
| S2 | **Phase 1 closes after the non-filer rework**, not before (JI, 2026-08-16). | set | `state_weights_phase1_summary.md:260-263` is the close-out checklist that three documents cite by line number, and it predates the sequencing decision — a reader who follows the citation to the authoritative checklist got the pre-decision instruction. Now carries a dated amend-in-place note pointing at the plan. `STATUS.md` item 1 already said "Do not close Phase 1 first." |
| S3 | The **Phase 1 §4 comparison harness is built**: `research/state_weights/scripts/sweep_state_weights.R` (part 1, hyperparameter sweep) and `validate_state_weights.R` (part 2, decision-relevant validation). `STATUS.md` item 1 points at them. | set | Both scripts self-describe in their headers as "Phase 1 §4 comparison harness, part 1/part 2", but `STATUS.md` item 1 and `state_weights_fit_issues.md` cited "the §4 comparison harness" whose only *spec* is `research/archive/state_weights_ml_alternative_2026-07-08_superseded.md` — an archived document whose premise the data refuted. The spec stays cited as the origin; the built thing is what you run. This also fixes `src/data/state_weights.R:30`, which had pointed at the pre-archive path since 2026-08-18. |
| S4 | The HT2 mirror is **`IRS-Ind`**. Documentation says `IRS-Ind`; `raw_data/IRS-GEO` remains a working symlink on the cluster, so existing data paths are not edited as part of a documentation change. | set | The repo was renamed 2026-08 and the code followed (`state_weights.R:57`), but three documents still named `IRS-GEO`, including one giving the GitHub URL. A glossary row in `research/CONVENTIONS.md` lets old citations resolve. **`validate_state_weights.R:151` read `raw_data/IRS-GEO/state/HT2` and was deliberately left alone** — repointing a data path inside a documentation commit is how a silent break hides. **Done 2026-08-27**, and outside a docs commit: the line now calls the sourced `ht2_path()` helper rather than rebuilding the path, which also removes a local variable that shadowed it. (The 2026-08-24 plan rewrite had dropped the task, so this row was the only surviving record of it.) |
| S5 | **Encoding coverage is 51/51 jurisdictions as of 2026-08-19.** The 9 / 21 / 22 / 30-state figures in five documents are point-in-time snapshots, not competing claims, and each now carries `true_as_of:` in its front matter. | set | `STATUS.md` says 51 encoded and "nothing is unstarted"; `STATE_ENCODING_REVIEW_2026_08_11.md` (30 states) asserts "by that bar **no state is complete**", `CODE_REVIEW_2026_07_17.md` says 22, `elderly_retirement_provisions.md` 21, `child_credit_survey.md` 9 — and three were cited as live companions by `STATUS.md` itself. Reviews are immutable records of a moment; the fix is to date them, not to rewrite them. |

## Per-state status

| ID | Decision | Status | Rationale / notes |
|----|----------|--------|-------------------|
| S6 | **`research/state_tax/state_parameter_rollout.csv` is the single per-state status surface.** Source packets carry `Status: see ../state_parameter_rollout.csv` and a `Last updated:` date; the harness README carries no done-list; reviews carry `true_as_of:`. | set | Five surfaces were tracking per-state status in five incompatible vocabularies: the CSV, the 51 packets' free-text `Status:` lines (five formats, including `nd.md` and `sc.md` transcribing the CSV's own four columns inline), the 39 cross-model report verdicts, the harness README's embedded "Status as of 2026-07-19" done-list, and a review appendix. The CSV wins because `src/tests/test_state_tax_law.R` already validates the code against it, and `state_parameter_workflow.md` §Validation gates already defines its vocabulary. The reports keep their per-state verdicts — they are the *evidence* a row is `done`, which is a different thing from the row. |

## Survey data and imputation

| ID | Decision | Status | Rationale / notes |
|----|----------|--------|-------------------|
| S12 | **Census-derived tax variables are used to VERIFY our work, never to produce it.** (JI, 2026-08-23.) On the CPS ASEC that means `FILESTAT`, `ADJGINC`, `TAXINC`, `FEDTAX`, `STATETAX`, `EITCRED`, `FICA` and the rest of the Census tax model's outputs, and `DEPSTAT` where it is used as a filing-unit pointer rather than a relationship. They may appear in a diagnostic, a comparison table or an acceptance check. They may not enter a tax unit's construction, an income concept, a model's covariates, a calibration target, or any imputation. The same rule applies to ACS equivalents. | set | Research pass A (A2) found three independent disqualifications, each sufficient on its own. **Circularity:** `ADJGINC`/`TAXINC` are statistically matched to the SOI PUF — the same file this workstream exists to correct — so using them would fit the file to itself. **A vacuous gate:** `FILESTAT`'s filer *count* is calibrated to administrative filing totals (O'Hara's $2,000 floor exists for that purpose), so adopting it would make the C6 acceptance test — does our filing model reproduce administrative filing rates — pass by construction and measure nothing. **It is wrong where it is not circular:** head-of-household is 41.5% short in TY2022, MFS does not exist at all, `FILESTAT` is broken outright in TY2020–21 (non-filing adults 42.9M → 11.7M → 11.5M → 44.0M), `DEPSTAT` is broken in TY2014, and 10.8% of its dependents point at someone the same model codes as a non-filer. The constructive half of the rule is that these variables are *excellent* verification: an independent estimate we did not fit to is exactly what an acceptance gate needs, and A2's fourth finding — that the ASEC-versus-anchor non-filer gap **is** the group-quarters universe difference, closing to 0.03M in both anchor years — was found precisely by using them that way. |
| S13 | **The DINA non-filer append is REPLACED, not patched: build the national non-filer population from the CPS ASEC and calibrate it to the residual anchors.** (JI, 2026-08-24.) Group D's V1/V2/V3 — the age draw, the weight rake and the aging fix — are **dropped**. D0 (the anchor age shape) survives as a *validation target* rather than an input; D4's id-order assertion survives because a new builder has the same positional-random-number hazard as a rake. | set | Measured 2026-08-24, the DINA non-filer file is not patchable. It carries **65%** of the anchor's adult mass with **77%** of the shortfall in 18-44 alone; `fiint`/`fidiv`/`fikgi` are **exactly zero** for every non-filer; wages are **24%** of what SSA and the SOI W-2 study jointly imply; and `ageprim` is **three values for everyone**, substantially an income-pattern proxy rather than an age (bucket 65 is 89.5% SS recipients and 0.0% wage earners). The composition cannot be repaired by reassignment either: 11.71M of bucket 65's 13.09M hold **old-age** Social Security against an anchor of 11.87M at 65+, so only 26% of the required movement is coherent. ASEC beats DINA on the dimension that matters — wages 1.011x SSA HI, dividends 0.449 of SOI, interest 2.624 — against 24% / 0.000 / 0.000. **Counter-argument recorded rather than hidden:** Treasury, the IRS and JCT all abandoned survey-based non-filer construction, the IRS because ASEC non-filer income "still fell short of the income reported to the IRS by third parties" (~17% undercount against a 50.7M target). That is a *count* problem and the count is calibrated to the anchor; DINA is worse on income than the method they rejected. **The gap that must be designed in, not discovered:** the ASEC has 0.27M group-quarters persons against a PUF/DINA universe that includes them (8.15M, 16.8% of the residual, 42% in SD), so GQ non-filers come from the ACS via `classify_gq()` and §3.0's universe invariant must be asserted. |
| S14 | **The DINA sex split is replaced too, so the DINA interface leaves Tax-Data entirely.** (JI, 2026-08-24.) `demographics.R:27-37` targets DINA male shares for unmarried units in 8 cells (`filer` x `has_kids` x `employed`). Under S13 the four `filer = 0` cells disappear — the ASEC observes `SEX`. The remainder is anchored to **SOI W-2 study Table 1** (`raw_data/IRS-Ind/national/w2/w2_all_2017.xls`, full Table 1 family for the base year), with the nonjoint male share derived as (1.A all taxpayers by sex) − (1.D joint-return taxpayers by sex). | set | DINA's `female` is a *modelled* variable on a modelled file, so targeting it is the same circularity S12 rejects for `ADJGINC`/`FILESTAT`. **Measured 2026-08-24**, the cells are not decorative: `p_male` runs 0.2476 to 0.6454 across the eight. But the variation is not where it was assumed. The `employed = 0` filer cells are **0.68%** of unmarried units, so they need no source of their own; the dominant dimension is **`has_kids`, a 19.3pp swing among employed filers** (0.4968 childless vs 0.3033 with children — single parents are ~70% female) covering 81% of unmarried units. **No admin source crosses sex with children:** all 51 sheets of the W-2 workbook were checked, and sex appears only against age, wage size, AGI size and joint-return share, never against "Return and Earner Type". So the W-2 study anchors the *marginal* male share and the ASEC build supplies the *kids split* — admin owns the level, survey owns the shape, as everywhere else in this workstream. |
| S15 | **The residual anchors take their LEVEL from Pub 1304 and their STATE SHARES from HT2, with the out-of-state filers removed from the level.** (JI, 2026-08-27; **note 2026-09-11, S21(b): the removal is a Census-PEP-basis construct; on the ssArea partition those filers are in-universe and stay filing adults, and the anchor is unchanged.** **implemented the same day** in `02_build_residual_anchors.R`, anchors now 48.470M (2017) / 47.803M (2022), state-sums-to-national asserted at build time, both bases kept as columns, out-of-state spread pro rata across age bands as a recorded assumption.) Formally: `residual_st = pep_st − (T1.6_filing_adults − out_of_state) × ht2_filing_share_st`, with the qualifying-surviving-spouse double-count corrected in the same change. Also settled the same day: Mok's omitted education group is **high school to some college** (approved for implementation, JI to confirm against the paper); the support test follows **TRIM3's** definition; the subfamily rule is written **once** as a survey-agnostic helper. | set | The two anchor files were built on two different sources for one quantity and nothing reconciled them, so the 51 states summed to **−1.36% (2017) / +2.14% (2022)** of the national anchor, sign flipping between the anchor years — and the fit targets the state file while every document quotes the national one. Measured in `state_weights/notes/anchor_basis_comparison.md`: taking the level from Table 1.6 and the shares from HT2 forces agreement by construction, inherits an adults-only universe the HT2 basis cannot have (it has no age, so it counts 2.1–2.2M under-18 filers as adults), and moves **no state outside its own tolerance** in either year. The out-of-state correction rests on SOI's Other Areas footnote — *"returns filed from Army Post Office and Fleet Post Office addresses by members of the armed forces stationed overseas; and returns filed by other U.S. citizens abroad"* — which places those filers outside the Census resident population that the anchor subtracts them from. It is a real ~1.0–1.2M change and is consistent with the filer side, which already carries 53 jurisdictions. With the QSS correction the two universe fixes raise the anchor **+2.4% to +3.2%**. **What the same pass could not close:** the 0.3–0.5% disagreement between the two SOI routes does **not** decompose into named universe differences — the remainder reaches 0.9% with the sign flipped — so `E_FILING_ADULTS = 0.005` is not conservative. A third construction from Pub 1304 Table 2.3 agrees with Table 1.6 to −0.057% against HT2's −0.370%, which localizes the remainder to an HT2-versus-Pub-1304 family difference rather than to Table 1.6. **And what implementing it did NOT do, measured after:** the placement metric barely moved (fitted adult shares against anchor shares, MARD 10.54% → 10.44%, 15 of 51 states inside 5% either way) — which Part B had already implied, since A and B differ by a uniform scale factor and the change sat inside every state's tolerance. "Inside tolerance everywhere" and "cannot move a share metric" are the same fact. This decision buys consistency and universe correctness, not fit. |
| S16 | **The two ASEC 2019-redesign asymmetries are settled, and neither needs data we do not have.** (JI, 2026-08-27.) **Retirement income** is harmonised by IPUMS's own documented mapping: `INCRETIR` for income years 2014–2017, and `INCRET1 + INCRET2 + INCPEN1 + INCPEN2 + INCRANN` for 2018–2024. **Capital gains** (`INCCAPG`) exist only from income year 2018, so D-A5's including-gains measure is built for TY2022 and its absence recorded for TY2017. **`SCHLCOLL`** is added to the common extract — the one genuine remaining gap. | set | The design note's §2.7 had diagnosed the retirement break correctly and quoted IPUMS's *"INCRETIR is NOT comparable before and after 2019"*, and A2b pulled the components on 2026-08-23; what was never written down was the bridge. IPUMS defines it: pre-2019 `INCRETIR` is retirement accounts **plus** pensions **plus** annuities at ages 15+, while post-2019 it is retirement accounts only at ages 58+. **Verified against both anchor years rather than assumed** (adults 15+, ASECWT, NIU codes read from each year's DDI): `INCRETIR` alone runs \$570.6B / 22.89M in TY2017 against \$221.5B / 11.12M in TY2022, i.e. **−61% dollars / −51% recipients** — dollars and recipients moving in opposite proportions, the signature of a definitional break. The harmonised series gives \$773.4B / 28.77M for TY2022, **+35.5% / +25.7%**, or +6.3%/yr nominal with the two moving together, which is what a population-and-asset trend looks like. On capital gains the design note's §5 row was wrong twice: `CAPGAIN`/`CAPLOSS` exist only for 1992–2008 **and** were Census tax-model output, so S12 would bar them even in range. `INCCAPG` is the real, survey-collected variable, worth \$170.5B and 12.03M recipients in TY2022 (1.2% of adult `INCTOT`). Because Mok's coefficients are scored on the **excluding**-gains measure, which exists in both anchor years, **the filing model is unaffected and the asymmetry falls on the benchmark** — which is why this is a recording decision rather than a modelling problem. `INCALIM` is a documented dead end: alimony folded into `INCOTHER` from 2014, so it is present but not separable for our whole window. | 
| S17 | **The self-employment margin cannot be hit at projected target levels, and the gap is ACCEPTED and reported rather than modelled away.** (JI, 2026-08-30.) `pub5785_hazard()` rakes to Publication 5785 Table 3's seven characteristic shares. Six land within ~0.2pp in every year. Self-employment lands within 0.2pp in the three **published** years and misses by **1.1–2.4pp** in every **projected** year. No adjustment is made: the residual is written per year per characteristic to `results/hazard_margins_{year}.csv`, the builder names the failing margin in a warning, and downstream analysis of self-employed non-filers carries this as a stated bound. | set | **Not the solver and not the cap.** Sensitivity measured on TY2022: raking sweeps 50 → 1000 moved the miss 3.38pp → 3.08pp, and lifting the probability cap 0.95 → 0.999 moved it only to 2.23pp. **It is the joint distribution.** Publication 5785's obligated non-filers are **44.7% self-employed and 66.0% wage-earning**, so at least 10.6% of them must be both; the ASEC's above-threshold population is **9.7% self-employed with 5.5% holding both**. Reweighting redistributes the joint distribution that exists and cannot manufacture one that does not — the household-survey blind spot for informal and gig self-employment landing squarely on the population that fails to file. **The level matters, and this is the part that constrains how the gap should be read:** in TY2014–2016, where the target is Publication 5785's own published column, the margin is hit to −0.10/−0.10/−0.20pp. It opens only once the projected level runs 10–15% above what those years demanded (TY2018 −2.42pp at the widest). So part of the residual belongs to the projection rather than to the survey, and it would shrink if the projected level were revised down. **Why not fix it.** Three options were weighed. Imputing informal self-employment into the above-threshold population would change the population to satisfy a constraint rather than because we believe it, which is the wrong direction for a file whose value is that its demographics are observed. Requesting a wages × self-employment cross-tabulation from the study's authors would resolve the incompatibility directly and remains available, but it is an external dependency on an unknown timetable and nothing should wait on it. Accepting and reporting keeps the file honest about a real limit. **What it bounds.** Self-employment is the characteristic most associated with owing tax and not filing — no withholding, and a return that costs more to prepare — so any analysis of compliance or of a filing-threshold reform inherits this understatement, and should cite the per-year residual rather than assume the composition matched. |
| S18 | **How the non-filer file is aged, in three parts.** (JI, 2026-08-30.) **(a) Income grows on the existing shared factors.** No filer dimension is added to `factor_ledger` for now. Recorded as future work: non-filer wages sit at the bottom of the distribution and the SOI per-return average that grows them is dominated by the top, so growing wages differentially across the income distribution is the improvement to make when a series supports it. **(b) Weights target the OBSERVED residual count of non-filing adults through 2022**, the last year the inputs reach, then phase into the demographic target over five to ten years rather than switching at the seam. **(c) The non-filer file is REBUILT for every year through 2022 and only aged beyond it** — we have the data to rebuild that the PUF does not. | set | On (a): the only wage series in the stack are `gdp_wages` and `awi`, both aggregate, so a distributional growth path would have to be sourced or derived first — the work is real but it is not blocked on a decision, it is blocked on an input. Social Security, which is roughly half of non-filer income, already grows on `outlays_mand_oasdi` and is unaffected. On (b): the observed residual moved in ways demography cannot produce — 48.47M (2017), **40.77M (2020)**, 46.46M (2021), 47.80M (2022) — so using it where it exists is strictly better than a demographic proxy; the phase-in exists because a hard switch at 2022 would put a discontinuity in the middle of the projection window. On (c): measured drift from `12_aging_check.R` is +1.2% on the level at one year but −5.2% at five, and the age composition swings sign (+15.4% in 2019, −7.0% in 2022) so no correction factor fixes it. Rebuilding is cheap now that every script is year-general. **The ceiling is 2022 and it is set by the inputs, not the survey**: HT2 ends at 2022 and Pub 1304 Table 1.6 at 2023, and the ASEC itself reaches income year 2024. **Premise updated 2026-09 (the decision is not): SOI published HT2 TY2023 in August 2026 and it is mirrored, so HT2 no longer ends before Table 1.6 — both end at 2023 and the input ceiling that set this decision has moved up a year.** Nothing was rebuilt: the anchor pair is still 2017/2022, every committed anchor is a 2017 or 2022 product, and `02_build_residual_anchors.R` keeps `HT2_REF_YEAR = 2022` as the latest year with a committed anchor file. What changed on its own is that a TY2023 run now takes that script's MEASURED branch rather than carrying the 0.59% level corrections from 2022. Whether to move the pair to 2017/2023 — ≈15 scripts' `ANCHOR_YEARS`, a full anchor rebuild, and re-verification — is open and unowned. |
| S19 | **The handoff-year basis is CBO's Social Security area universe, at the handoff year only.** (JI, 2026-08-30.) **Superseded in part by S21 (2026-09-11): the per-band scale on the 2023 pool is retired and the partition is built for every year with a named not-in-universe block; the self-preservation mechanism described here is unchanged.** The projection that carries every weight forward runs on Macro-Projections' demographic cells, verified this session to BE CBO's ssArea population to the person. At T = 2023 the partition — filing adults + non-filer target + claimed-dependent netting = adults — is built and asserted per age band on that universe (`nonfiler_pool/15_ssarea_alignment.R`), so cell scaling by `N_b(y)/N_b(T)` preserves it exactly in every later year. The 2014–2022 anchors and all state work stay on Census PEP resident population. The filer base moves to 2023 on observed Pub 1304 T1.6 counts (returns per adult .6314 in 2020, .6008 in 2023 — the demographic handoff lands after the spike has reverted, not inside it). | set | The PEP↔ssArea wedge is ~1.5% on adults but **2.6–11.7% on the residual by band** (2.6% in 18_25; the five bands 26_34–65p span 5.6–11.7%), because the residual is a small difference of large numbers — which is why the universe choice is a decision and the wedge a published table (`ssarea_wedge_2023.csv`), not a footnote. The partition cell is the AGE BAND, deliberately: the non-filer weight path scales by band, so band closure at T is the exact self-preservation condition — and a band×married partition is NOT identifiable from T1.6, whose primary-band convention piles both joint adults into one band; the unmarried 35_44 cell proves it by arithmetic (slack 0.96M against a larger netting). The 2023 pool takes a per-band scale of 1.061–1.134 to close on ssArea — applied at the 2023 emit in `05_emit_pool.R` (2026-08-31), exactly once, gated pre and post against the alignment table. Mortality needs no accompanying change: records are age slices, cells carry deaths on the weight side (cohort ratios track CBO survival within 0.5pp), and `q_death(frozen age, actual year)` is the correct lookup — cohort-aging the file would double-count mortality. |
| S21 | **The partition is built on ONE universe for every year, not only at the handoff year.** (JI, 2026-09-11: (a) adopt, (b) adopt, (c) named block.) Implemented the same day: `15_ssarea_alignment.R` runs per year 2017–2023 on the Macro-Projections cells, `05_emit_pool.R` applies no scale to any year, every year publishes `ssarea_partition_{year}.csv`, pool vintage **2026091201** (2017–2022 byte-identical to 2026090101; 2023 unscaled, 37.45M adults), Tax-Data pins the 2023 level to the built 2023 pool. **Measured caveat on (c), found while building:** the block is NOT only geography. Macro-Projections' cells are a January-1 population (SSA Trustees' series before CBO's first projected year) and the anchors' PEP is Census July 1, so "ssArea − PEP" by band carries six months of cohort aging (65+ ≈0.9M lower on Jan 1; 18–25 likewise) and series-vintage differences; in 2017–2019 the block is negative in 18_25, 55_64 and 65p, in 2020–21 in 35_44, positive in every band from 2022. The table publishes `block_timing` (−(PEP_y − PEP_{y−1})/2) and `block_universe` (the remainder) so the definitional piece is visible: 1.6–1.7M in 2017–2019, 2.1–4.1M in 2020–2023. Positivity is asserted at the handoff year only. Separating Puerto Rico/territories/abroad properly needs a Jan-1 resident series or an explicit PR adult series — open follow-up. Original proposal text follows.  Under S19 the 2014–2022 anchors are Census PEP resident population and only T = 2023 is CBO's Social Security area (ssArea). Measured 2026-09-11 in Tax-Data's output: non-filer adults step 35.7M (2022) → 41.2M (2023), +15%; the observed residual explains +7.6% (47.80M → 51.43M, real: filing adults fell while population grew) and the universe switch the rest. Under Block E, which takes 2017–2022 from the PEP-basis pools and 2023 from the ssArea-scaled one, the step is larger still (35.05M → 41.23M, +18%). Proposal, three parts: **(a)** build the band partition on Macro-Projections' demographic cells (verified equal to CBO ssArea to the person in 2022–2023) for every year 2017–2023, so `15_ssarea_alignment.R` runs per year and the Tax-Data anchor becomes per-year and trivial; **(b)** on that universe do NOT remove the S15 out-of-state filers (APO/FPO and citizens abroad, ≈1.2M adults) — they are inside ssArea, so on ssArea they are filers, not non-filers; the QSS double-count correction stays; **(c)** decide the remaining wedge (≈2.5M adults: Puerto Rico, the territories, other citizens abroad — inference from ssArea's definition, to verify against CBO/SSA documentation) as either represented non-filers or a NAMED not-in-universe component carried alongside the identity; recommendation is the named component for a federal income-tax model (IRC §933 puts PR-source income outside the base), with the EIP/2021-CTC counterargument recorded (those reached PR). Consequence of adopting: no universe step at 2023; the +7.6% observed step remains because it is real. | set | Measured against `national_anchor_{2022,2023}.csv`, `ssarea_alignment_2023.csv`, `ssarea_wedge_2023.csv` and Tax-Data output vintage 2026083111. The S19 rationale — the partition self-preserves under aging because cells scale by N_b(y)/N_b(T) — holds for ANY partition defined at T on the projection universe, including one that carries a named not-in-universe block, so (c) costs nothing in projection exactness. Macro-Projections' historical cells for 2017–2021 predate the CBO file's first year (2022); **verified 2026-09-11** in `Macro-Projections/src/macro-projections.R` (section A, population): those years come from the SSA Trustees' Report `SSPopJan.csv` by age and marital status, i.e. the Social Security area population, so the universe is ssArea in every year and (a) can be built without a bridge. |
| S23 | **OPEN, for JI: random draws in both repos must be keyed by record ID, not by row position and record count.** Found 2026-09-12 by the first run of the federal validation battery, which FAILS its central test: 187 of 209 filer-gated 1040 columns move between Tax-Data vintages under identical tax law and a non-filer-only change. Two mechanisms, both identity-blind. **(a) Tax-Data, dominant:** `src/imputations/*.R` draw with `runif(nrow(.))` / `sample_n()` under one `set.seed(76)`, so the number of draws consumed scales with the record count and every module after the first sees a shifted stream when the base changes size. 46 of 188 columns differ at the record level for the SAME 207,692 filers between 2026091118 and 2026091119; in aggregate the QBI sole-proprietor wage bill moves −3.9%, childcare expenses +2.0%, tips +0.7%. Those enter AGI, and AGI gates the 2020 UI exclusion and the excess-business-loss limitation, so lines whose inputs are record-identical (`ui`, `sole_prop`, `farm`) still move. **(b) Tax-Simulator:** `sample_frac(size = pct_sample)` at `config_parser.R:222` selects nothing at a full sample (its only consumer, `id %in% sample_ids`, ignores order) but consumes RNG proportional to the record count, so every vintage of a different size gets a different stream; the nine `random_numbers` columns are also drawn sequentially from one stream. Controlling (b) alone leaves the 187 unchanged and only tames the extremes (`n_excess_bus_loss` 67% → 3.5%). **Proposal:** derive each draw from a stream seeded on `(record id, module, seed)` in both repos, as one coordinated vintage. **Cost, stated:** every published number changes once. Revenue-level impact of the current noise is small (income tax 0.078%, payroll 0.60%) but §4a is an exact-equality test by design, and until this lands no design that changes the record count — Block E included — can be certified non-filer-only. Evidence and reproduction: `nonfiler_federal_validation_findings.md`; measurement instrument on Tax-Simulator branch `rng-control`, not for merge. **SET and IMPLEMENTED (JI 2026-09-13: "go").** Tax-Data `src/imputations/rng.R` (fixed 1e7 id space per stream, explicit append-only stream table, global seed saved and restored, 11 standalone tests) with 25 converted call sites across 13 modules; Tax-Simulator `src/misc/rng.R` with `config_parser.R` rewritten (the no-op full-sample shuffle removed, per-id draws, ids kept in FILE order so the positional `bind_cols` still aligns). Invariance test, same code on 374,630 vs 1,399,234 records: **46 → 10 → 1** moving filer columns. **(c) A THIRD, distinct mechanism closed the last one (2026-09-13): the model FIT was positional too.** `quantregForest`/`ranger`/`drf` draw their bootstrap samples from the global stream, so a fit taken at a stream position that depends on the record count is a different forest — and keying the prediction by id cannot reach it, because the ensemble the id indexes into has already changed. `mortgage.R` carried a committed `estimate_models = 1` above its own `if (estimate_models)`, so it retrained on every run whatever the operator asked for and left the flag at 1 for `consumption.R` below it. Fixed by `model_seed()`/`with_model_seed()` in rng.R, applied in all three `train_or_load_*` helpers and in mortgage.R (`d23889c`). Wealth remains non-invariant BY DESIGN (the tilt is a joint fit per bucket), documented in code. | set | Predicted in the review brief §9 under "what design A does not fix", and not specific to Block E: removing the DINA append already moved the same families (consumption ≈−3%, tips ≈−9%). Design B (one cross-section) avoids it only by giving up the annual rebuild S18(c) requires. |
| S22 | **Block E is design A, the union base: every published pool year is appended to the 2017 base, each record tagged with its `base_year`, carrying weight only in the year(s) it represents.** (Implemented 2026-09-11 on JI's instruction to proceed with Block E after S21; A is the design the review brief recommended and the only one that keeps Tax-Simulator's contract — same rows, same order, every year — intact. JI to confirm the choice against B and C.) Mechanics: pools are moved into disjoint id blocks (`id + (year − 2017)·1e6`, `offset_nonfiler_ids()`); `weight` in the base is the 2017 weight (zero for later pools) so Phase 1 sees a 2017-weighted population; `materialize()` applies `factor(y)/factor(base_year)` so a record at its own year reproduces the published file; `weight_ledger` gives the year-y pool its own weight in y, zero elsewhere, and grows the last pool (2023) forward on the S18(b) band series relative to 2023; wealth is imputed at 2022 only for records with positive weight in some year ≥ 2022 (filers, the 2022 and 2023 pools), others get 0; the S21 partition is asserted for every pool year (non-filer side equals the pool, hard). The 2023 handoff anchor of the same morning is retired — the level is the pool by construction. | set (JI to confirm) | Costs accepted: non-filer rows per file 166,938 → 1,191,542 (about 3× the file, ~1 GB per year); Phase 1's positional draws shift once more for filers (measured in the E2 tripwire); later pools' nominal dollars rank against 2017 cut points in Phase 1 (second-order; deflate-at-append is the fix and needs the income factors before the append). Verified: `materialize.R` tests 17–18 (base-year division), contract test 23/23, end-to-end run recorded in the branch notes. Tax-Simulator needs no change; `distribution.R` already filters `weight > 0`. Follow-ups: key Tax-Data's Phase 1 draws by id (closes the 2024+ filer drift); promote the pool build into the numbered pipeline. |
| S24 | **Tax-Data emits each year's LIVE records only (design C), not the whole union base (design A).** (JI, 2026-09-14.) A and C are the SAME build through Phase 3 and differ only in what `write_outputs.R` writes; C filters `weight > 0` after the Forbes splice. **Why:** under A about 73% of every year's file is a record with zero weight in that year (live share 26.8-28.1% in every year 2017-2097), and Tax-Simulator never drops them -- `filter(id %in% sample_ids)` at its `run.R:352` is the ONLY filter in its run path -- so A ran the whole calculator on ~1M inert rows per year, on every run, forever. **Measured before adopting:** 78G -> 25G, and output identical to the last bit -- 0 of 210 columns move on `totals/1040.csv`, 0 of 36 on payroll, 0 of 8 on receipts, 0 of 210 by AGI; the `by_agi` NaN edge case did not materialise (160 rows both sides). Separately, `state-tax` vs the Tax-Simulator change on the A vintage is also 0 of 210, which is what makes the result attributable to the emit rule alone. **Cost:** the fixed-id contract goes -- the record set now varies at 7 year boundaries of 81 (from 2024 the live set is constant), so Tax-Simulator's membership becomes `in_subsample(id, pct)`, a rule keyed on the record, and `random_numbers` is built per year from that year's ids, retiring the positional `bind_cols`. Preflight check 1 is rewritten accordingly. `TAXDATA_EMIT_LIVE_ONLY=0` restores A for reproducing the A/B. Scope, evidence and method: `nonfiler_design_c_scope.md`. | set | No PUF filer is EVER zero-weight, 2017-2097, so the rule only ever drops pool records. Nothing longitudinal breaks: Tax-Simulator holds no record-level state across years -- the only id-keyed join is `by = c('id','year')` in `src/sim/behavior.R`, within-year, and years run independently under `mclapply`. Loss carryforwards are explicitly not modelled. Design A remains built and verified on `block-e` if this is ever revisited. **Unverified premise, accepted on JI's instruction:** no Budget Lab consumer outside Tax-Simulator reads Tax-Data output assuming stable ids across years. |
| S25 | **Forbes synthetic ids are renumbered into a reserved block inside the shared id space, rather than widening the space.** (JI, 2026-09-14.) `make_forbes_id()` was `year * 1e6 + rank`, putting the 2022 cohort at 2,022,000,001..717 against an id space of 1e7. Now `FORBES_ID_BASE 9e6 + (list_year - 2020) * 1e4 + rank`: the four cohorts land at 9,020,001..9,050,935. **Why it surfaced:** those rows had NEVER reached Tax-Simulator -- 717 from the 2022 list, 749/813/935 after -- because `sample_ids` came from the 2017 file, which contains none of them, so `run.R:352` dropped every one, on `main` too. S24 made membership a rule on the record, they were admitted, and `draw_by_id()` refused them: a record outside the id space has no defined draw. **Why renumber rather than widen:** raising `RNG_ID_SPACE` moves every record's draw in both repos and would pay S23's switchover cost a second time; these ids are synthetic, so renumbering costs only the rows themselves. **Layout asserted, not assumed:** filers below 1e6, pool year y at [1e6*(y-2016)+1, 1e6*(y-2015)] reaching 8e6 at the 2023 ceiling, Forbes from 9e6 in 1e4 blocks per list year -- leaving a spare pool block at 8e6..9e6 and room to list year 2119. `make_forbes_id()` stops on rank overflow, on a pre-epoch year, on collision with the pools, and on exceeding the id space. | set | This ADMITS the Forbes records to the simulation for the first time, which moves the top of the income distribution. That is the point, and it is why it is logged as its own decision with its own before/after rather than riding inside S24. |
| S20 | **For 2020–21 the above-threshold hazard level is deflated per age band by OBSERVED pandemic excess filing.** (JI, 2026-08-31.) The projected Pub 5785 level (12.39M/12.49M units, rate held at TY2016) is infeasible against the residual anchors: the stimulus filing surge collapsed the residual count of non-filing adults (48.5M → 40.8M in 2020) while the held level did not move, and band 18_25's held contributions exceed its entire target (−0.469M in 2020, −0.096M in 2021). The surge is treated as an exogenous, observable draw from the non-filer stock: per band, excess filing X_b = (rate_obs − rate_cf) × PEP adults, with the counterfactual a linear interpolation of band filing rates between 2019 and 2023 (the nearest years outside the excursion); attribution θ_b = the above-threshold share of the band's non-filing adults, interpolated from the calibrated 2019/2023 endpoints; deflator m_b = 1 − θ_b·X_b / hh_above_b, clipped to [0.05, 1] and capped by the identity's closed-form feasibility ceiling (`16_pandemic_filing_adjustment.R`; applied in `pub5785_hazard()` after the trend rake and level solve, so the external-level identification survives with its pandemic adjustment observed rather than assumed). Central deflators: TY2020 m = 0.190/0.767/0.777/0.863/0.885/0.810 by band, level 12.39M → 8.89M (−28%); TY2021 0.488/0.912/0.979/1/0.999/0.898, → 10.89M (−13%). No ceiling binds. | set | The stated assumptions are the counterfactual endpoints and θ. Two report-only sensitivity columns bracket θ (the identity's own minimum deflation; θ = 1), and the netting brackets stay central-only as in every non-2017/2022 year. Not re-raked after deflation — the characteristics correlate with age, so re-raking would partially undo the band-specific deflation; the margin drift is committed to `hazard_margins_{y}.csv` (married +4.2pp worst, 2020). Substantive findings: θ rises steeply with youth (0.78 in 18_25 — young non-filers are mostly ABOVE-threshold), and 65p carries 2.09M excess senior filing in 2020 (m = 0.81). 2022 also shows ~0.77M excess in 18_25 under the same counterfactual but is feasible and published — deliberately out of scope. Every other year is bit-identical (NULL deflator path; 2017 re-score gates verified byte-identical). Aging-vs-rebuild for the pandemic years, now measurable: aged-from-2017 misses the built 2020 by −23.3% adults / −33.5% wages — the strongest evidence yet for S18(c)'s rebuild rule. |

## Documentation scheme

| ID | Decision | Status | Rationale / notes |
|----|----------|--------|-------------------|
| S7 | Research documents live in a top-level **`research/`** tree, one workstream per directory, **exactly one `role: plan` per workstream**, with roles as front-matter metadata rather than folders. `docs/` keeps the shipped model documentation. | set | `other/state_tax_research/` had reached 212 tracked files across ten filename patterns, with 20 documents tracking outstanding work. Conventions and drift checks in `research/CONVENTIONS.md` and `research/README.md`. |
| S8 | **Markdown is the source of truth; Word documents are dated snapshots.** Renders are cut by `research/tools/render_release.R` into `research/releases/YYYY-MM-DD_<slug>.docx`, stamped with the render date, branch and commit. Comments on a release are welcome; edits are applied to the Markdown, not carried back. Word-*native* documents (`research/docx_sources/`) are exempt — they were never Markdown. | set | Two undated renders had gone stale beside their sources unnoticed: `state_tax_implementation_plan.html` (six weeks stale, and it had lost the amendment saying its own §2.1 was superseded) and `state_weights_phase1_summary.docx` — whose staleness `archive/README.md` had predicted in writing and which happened anyway. Per-file rmarkdown `output:` blocks are removed; the Knit button is no longer the mechanism. |
| S9 | A living document is **amended in place** and carries a `## Revision history` tail. A dated snapshot goes to `research/archive/` on exactly three triggers: an external review going out, a whole section being rewritten rather than amended, or the document becoming superseded or fully executed. | set | Git holds every intermediate revision, so snapshotting each edit would add clutter without adding recoverability. The three triggers are the cases where a *named*, browsable version matters. Naming follows the convention already in use: `{basename}_{YYYY-MM-DD}_{reason}.{ext}`. |
| S10 | The **unified non-filer + state-weights plan is one in-repo document**: `research/state_weights/plan.md`. It merges the 2026-08-18 out-of-repo plan with the 2026-08-19 to-do; the to-do wins on every fact. Both predecessors are archived. | set | The fuller plan (1,163 lines) lived at `~/.claude/plans/review-the-new-non-filer-starry-rivest.md` — outside version control, cited by nothing in the repo, and stale in three specific ways, while the repo declared a 595-line to-do the "single operational entry point". Two same-scope documents is the pattern that caused the drift the corpus already had. Step 4 of the imported plan became `nonfiler_federal_validation.md` (`role: procedure`) rather than a second plan. |
| S11 | The **cross-model harness is split by what the test reaches for**: `src/tests/state/cross_model/` holds the PolicyEngine driver, its pinned requirements and the machine-read `known_differences.csv`; everything else — the CLI driver, results, reports, cache, bug reports, companion records — is in `research/state_tax/cross_model/`. | set | A `src/` test may not depend on a path in a tree whose defining property is that documents get archived and moved. Two constraints shaped where the line falls: nothing non-`.R` could previously live under `src/` at all (the recursive source walk in `main.R`, `slurm/{setup,common}.R` and the harness driver sourced *every* file), so those four walks are now filtered to `.R`; and **no executable R may live in `src/tests/state/cross_model/`**, because `run_cross_model.R` does its own recursive source of `src/` and would recurse infinitely if the walk picked it up. Hence the driver sits in `research/` while the driver's inputs sit in `src/`. |

---

## Revision history

- **2026-09-13** — S23 set and implemented in both repos. The last non-invariant column turned out to be a positional model FIT, not a
  positional draw; model training is now seeded by the model's name.
- **2026-09-12** — S23 proposed: id-keyed random draws in both repos, after the
  federal validation battery's first run failed its central test for a reason
  unrelated to the non-filer population. S22 (Block E as design A, the union
  base) recorded the day before.
- **2026-09-11 (later still)** — S22: Block E built as design A, the union
  base, on the `block-e` branch.
- **2026-09-11 (later)** — S21 set: (a) one universe every year, (b) out-of-
  state filers stay filers on ssArea, (c) named not-in-universe block. The
  block's measured composition (Jan-1/Jul-1 timing and series vintage, not
  only geography) recorded as a caveat; S19's scale retired; S15 annotated.
- **2026-09-11** — This file is now maintained in **Tax-Data** (`research/
  decisions_log.md`); the Tax-Simulator copy is frozen (see
  `research/state_weights/MOVED.md` there). S1–S6 concern the state-law
  workstream and are kept here as a mirror only. S21 proposed: one universe for
  every year of the partition.

- **2026-08-31** — S20: the 2020–21 above-threshold hazard level is deflated
  per band by observed pandemic excess filing (T1.6 against a 2019→2023 rate
  counterfactual, attributed at the above-threshold stock share), which is
  what makes those two years calibrable at all.
- **2026-08-30** — S19: the handoff-year basis — ssArea at T = 2023 only, the
  filer base on observed IRS counts through 2023, the band partition asserted,
  the wedge published.
- **2026-08-30** — S18: how the non-filer file is aged — shared income factors
  for now, observed residual counts through 2022 with a phase-in after, and a
  rebuild rather than an aging for every year the inputs reach.
- **2026-08-30** — S17: the self-employment margin gap is accepted and reported
  rather than modelled away, with the per-year residual committed to
  `hazard_margins_{year}.csv`.
- **2026-08-24** — S13 and S14: the DINA non-filer append is replaced by an
  ASEC-constructed pool (group D dropped), and the DINA sex split is replaced by
  SOI W-2 Table 1 plus the ASEC, retiring the DINA interface.
- **2026-08-19** — Created with S1–S11 during the move of the research corpus to
  `research/`. S1–S5 close contradictions found by the pre-move inventory; S6–S11 record
  the scheme itself.
